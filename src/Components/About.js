import React from 'react'
// import {withRouter} from 'react-router-dom'

const About = (props) => {
    // console.log(props.location.state[0])
    // let { Name,Password,Address,Mobile } = props.location.state[0];
    return (
      <div>
        <h1>This is About page</h1>
</div>
    );
}

export default About
